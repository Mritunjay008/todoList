# Leave empty for local development (uses Vite reverse proxy /api)
# For Azure deployment, set to your deployed .NET Web API URL, e.g.:
# VITE_API_BASE_URL=https://your-backend-api.azurewebsites.net
VITE_API_BASE_URL=
